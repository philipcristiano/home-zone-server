---

- hosts: physical
  roles:
  - philipcristiano.ntp
  - { role: omnios-zone, zone_name: repo, net_physical: repo1}

- hosts: all
  roles:
  - common
  vars:
    domain: home.philipcristiano.com
    nameservers:
    - 192.168.1.1
    - 8.8.8.8
